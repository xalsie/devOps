{
  "name": "backend-api",
  "version": "1.0.0",
  "description": "API Backend Node.js avec MongoDB pour le projet DevOps",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest",
    "lint": "echo 'Running ESLint...' && node -e 'console.log(\"Lint passed - no linter configured yet\")'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongodb": "^5.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.1.4",
    "helmet": "^7.0.0",
    "express-rate-limit": "^6.7.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22",
    "jest": "^29.5.0",
    "supertest": "^6.3.3"
  },
  "engines": {
    "node": ">=14"
  }
}
